name: Deploy to EC2

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Angular build
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build Angular
        run: |
          cd client
          npm install
          npm run build -- --configuration production

      - name: Move Angular to Go static
        run: |
          rm -rf server/static
          mkdir server/static
          cp -r client/dist/client/browser/* server/static/

      # Go build
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.22

      - name: Build Go
        run: |
          cd server
          go mod tidy
          go build -o app

      - name: Prepare EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            APP_DIR=/home/ubuntu/app/server
            mkdir -p $APP_DIR/static
            rm -f $APP_DIR/.env
            echo "DB_USER=${{ secrets.DB_USER }}" > $APP_DIR/.env
            echo "DB_PW=${{ secrets.DB_PW }}" >> $APP_DIR/.env
            echo "DB_HOST=${{ secrets.DB_HOST }}" >> $APP_DIR/.env
            echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> $APP_DIR/.env
            echo "OLLAMA_API_KEY=${{ secrets.OLLAMA_API_KEY }}" >> $APP_DIR/.env
            echo "VAPID_PUBLIC_KEY=${{ secrets.VAPID_PUBLIC_KEY }}" >> $APP_DIR/.env
            echo "VAPID_PRIVATE_KEY=${{ secrets.VAPID_PRIVATE_KEY }}" >> $APP_DIR/.env
            echo "CLIENT_URL=${{ secrets.CLIENT_URL }}" >> $APP_DIR/.env
            echo "ADMIN_EMAIL=${{ secrets.ADMIN_EMAIL }}" >> $APP_DIR/.env
            chmod 600 $APP_DIR/.env

      # EC2 전송
      - name: Copy files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          source: "server/app,server/static"
          target: "/home/ubuntu/app"

      # 서버 재시작
      - name: Restart server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            sudo systemctl daemon-reload
            sudo systemctl restart myapp.service
            sudo systemctl status myapp.service --no-pager
